@{
    ViewData["Title"] = "Iniciar sesion";
}

<div>
    @if (User.Identity != null && User.Identity.IsAuthenticated)
    {
        <p>Ya has iniciado sesión. Serás redirigido al inicio en uno segundos...</p>
        <script>
                    setTimeout(function ()
                    {
                        window.location.href='@Url.Action("Index", "Home")';
                    }, 3000);
                </script>
    }
    else
    {
        @* <form method="post">
            <div class="form-group">
                <label for="username">Nombre de usuario</label>
                <input id="username" name="username" required type="text"
                    class="form-control">
            </div>
            <div class="form-group">
                <label for="password">Contrasenia</label>
                <input id="password" name="password" required type="password"
                    class="form-control">
            </div>
            <br />
            <button type="submit" class="btn btn-primary">Ingresar</button>
        </form>
        <br />
        <a class="btn btn-outline-primary mt-2" asp-action="Register">Registrarse</a> *@

       
            <div class="authentication-wrapper authentication-basic">
                <div class="authentication-inner">
                    <!-- Register -->
                    <div class="card">
                        <div class="card-body">
                            
                            <!-- /Logo -->
                            <h4 class="mb-2">Bienvenido a Tienda Online 👋</h4>
                            <p class="mb-4">Tu tienda favorita para comprar.</p>

                            <form method="post">
                                <div class="mb-3">
                                    <label for="username" class="form-label">Nombre de usuario</label>
                                    <input type="text" class="form-control" id="username" name="username" placeholder="Enter your email or username" autofocus>
                                </div>
                                <div class="mb-3 form-password-toggle">
                                    <div class="d-flex justify-content-between">
                                        <label class="form-label" for="password">Contraseña</label>
                                        @* <input id="password" name="password" required type="password"
                                               class="form-control"> *@
                                        
                                    </div>

                                    <div class="input-group input-group-merge">
                                        <input type="password" id="password" class="form-control" name="password" placeholder="&#xb7;&#xb7;&#xb7;&#xb7;&#xb7;&#xb7;&#xb7;&#xb7;&#xb7;&#xb7;&#xb7;&#xb7;" aria-describedby="password" />
                                        <span class="input-group-text cursor-pointer"><i class="bx bx-hide"></i></span>
                                    </div>
                                </div>
                                <div class="mb-3">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" id="remember-me">
                                        <label class="form-check-label" for="remember-me">
                                            Remember Me
                                        </label>
                                    </div>
                                </div>
                                <div class="mb-3">
                                    <button class="btn btn-primary d-grid w-100" type="submit">Sign in</button>
                                </div>
                            </form>

                        <a class="btn btn-outline-dark d-grid w-100 mt-2" asp-action="Register">Registrarse</a>

                            @* <p class="text-center">
                                <span>New on our platform?</span>
                            <a class ="btn btn-outline-primary mt-2" asp-action="Register">
                                    <span>Create an account</span>
                                </a>
                            </p> *@
                        </div>
                    </div>
                    <!-- /Register -->
                </div>
            </div>
       
    }
</div>